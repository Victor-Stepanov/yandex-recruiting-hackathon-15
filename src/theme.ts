import { createTheme } from '@mui/material/styles';

export const theme = createTheme({
  typography: {
    fontFamily: [
      'YS Text',
      'Roboto',
      'Arial',
      'Helvetica',
      'Arial Unicode MS',
      'sans-serif',
    ].join(','),
  },
});
